export const environment = {
  production: false,
  // Relative path — the Angular dev server proxies `/api/*` to the ASP.NET
  // backend on `http://localhost:5228` via `proxy.conf.json`. This avoids
  // CORS during dev and keeps the same-origin model that production uses.
  apiBaseUrl: '/api',
  defaultPageSize: 20,
  // Set your own Syncfusion license key locally; never commit a real key here.
  syncfusionLicenseKey: '',
};
